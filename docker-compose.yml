# Удобная точка входа из корня репозитория (дублирует infra/docker-compose.yml)
services:
  backend:
    build:
      context: ./apps/backend
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    environment:
      PORT: "5000"
      CLIENT_URL: "http://localhost:5173"
