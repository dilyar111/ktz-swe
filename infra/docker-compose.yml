# Минимальный прод-образ API. Симулятор и Vite-фронт удобнее гонять локально: npm run dev
services:
  backend:
    build:
      context: ../apps/backend
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    environment:
      PORT: "5000"
      CLIENT_URL: "http://localhost:5173"
