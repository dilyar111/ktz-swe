# Опционально: переопределить URL API/WebSocket (по умолчанию http://localhost:5000).
# Рекомендуется задать в корневом `.env` репозитория — Vite загружает переменные с корня монорепо.
# VITE_WS_URL=http://localhost:5000
